const geminiConfig = require('../../config/gemini.config');
const { logger } = require('../../utils/logger');

/**
 * Gemini AI integration module placeholder for Phase 1 infrastructure
 */
const getGeminiConfig = () => geminiConfig;

module.exports = {
  getGeminiConfig
};
