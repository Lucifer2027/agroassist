const weatherConfig = require('../../config/weather.config');
const { logger } = require('../../utils/logger');

/**
 * OpenWeatherMap integration module placeholder for Phase 1 infrastructure
 */
const getWeatherConfig = () => weatherConfig;

module.exports = {
  getWeatherConfig
};
